export type UID = string;

export type StringTime = `${number}:${number}`;

export type ISO8601String = `${number}-${number}-${number}T${number}:${number}:${number}`;

export type DateString = `${number}-${string}-${string}`;
